%h1 List of treasures:
- if @treasures == nil
  %p There are not any treasures currently in the system.
- else
  %ul
    - @treasures.each do |t|
      %li
        = link_to t.title, t
        = link_to t.type.name, t
%div.btn-group
  %p= button_to('Add new treasure', { action: 'new' }, class: "btn-success")
  %p= button_to('Trashed treasures', { action: 'trash' }, class: "btn-success")
  %p= button_to('All available types', {action: 'index', controller: 'types'}, class: "btn-success")
  %p= button_to('Rent to someone', { action: 'new', controller: 'rentals' }, class: "btn-success")