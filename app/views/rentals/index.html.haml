%h1 List of rentals:
- if @lend_rentals.blank?
  %p There are not any lend treasures currently in the system.
- else
  %p= 'My treasures lend to someone:'
  %table.table.table-striped
    %thead
      %tr
        %th Treasure
        %th Type
        %th Start date
        %th Due date
        %th Returned
    - @lend_rentals.each do |r|
      %tbody
        %tr
          %td
            = r.treasure.try(:title)
          %td
            = r.treasure.type.try(:name)
          %td
            = r.start_date
          %td
            = r.due_date
          %td
            = r.return_date
          %td
            - if r.return_date == nil
              = button_to 'Mark as return', r, { method: :put }
            - else 
              = r.return_date
-if @rentals.blank?
  %p There are not any borrowed treasures currently in the system.
-else            
  %p= 'My borrowed treasures:'
  %table.table.table-striped
    %thead
      %tr
        %th Treasure
        %th Type
        %th Start date
        %th Due date
        %th Returned
    - @rentals.each do |r|
      %tbody
        %tr
          %td
            = r.treasure.try(:title)
          %td
            = r.treasure.type.try(:name)
          %td
            = r.start_date
          %td
            = r.due_date